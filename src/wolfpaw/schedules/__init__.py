"""HTTP surface for schedules (the recurring/one-shot triggers the agent
registers via `schedule_task`). The DAO lives in `wolfpaw.memory.schedules`;
this package exposes a read-only listing for the web app's Scheduled tab."""
